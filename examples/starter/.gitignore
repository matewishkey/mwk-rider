node_modules
dist
.astro
.env
.wrangler
# Generated by `wrangler types` from wrangler.jsonc — 500 KB of runtime types.
# `npm run check` regenerates it, so it never needs to be committed.
worker-configuration.d.ts
.DS_Store
# Generated by `npm run og`, which `npm run build` runs first. Regenerated
# from the content every build, so a committed copy could only ever be stale.
public/og/
